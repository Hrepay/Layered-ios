import Foundation

/// 이 파일을 같은 위치에 Secrets.swift로 복사한 뒤 실제 키를 채우세요.
/// Secrets.swift는 .gitignore에 등록되어 커밋되지 않습니다.
enum Secrets {
    /// 카카오 로컬(장소 검색) REST API 키 — developers.kakao.com 앱 키에서 발급
    static let kakaoRESTAPIKey = "YOUR_KAKAO_REST_API_KEY"
}
