{
  "name": "layered-rules-tests",
  "private": true,
  "type": "module",
  "description": "Firestore·Storage 보안 규칙 유닛 테스트 (에뮬레이터 전용, 프로덕션 미접근)",
  "scripts": {
    "test": "cd .. && firebase emulators:exec --only firestore,storage --project demo-layered \"node --test rules-tests/firestore.rules.test.mjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.10.0"
  }
}
